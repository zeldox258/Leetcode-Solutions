class Solution:
    def maximum_xor_of_two_nonoverlapping_subtre(self, nums: List[int]) -> int:
        cur = mx = nums[0]
        for x in nums[1:]:
            cur = max(x, cur + x)
            mx = max(mx, cur)
        return mx